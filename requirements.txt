# ============================================================================
# Administrador de Conexiones LAN — dependencias
# Python 3.11+
# ============================================================================

# --- Nucleo Fase 1 (descubrimiento + API + dashboard) ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
scapy>=2.5,<3.0            # escaneo ARP (requiere Npcap en Windows)
APScheduler>=3.10,<4.0     # escaneo periodico programado
pydantic>=2.6,<3.0

# --- Opcional Fase 1 (descubrimiento extendido por nmap) ---
# Requiere el binario 'nmap' instalado en el sistema.
python-nmap>=0.7.1

# --- Fase 3: seguridad y utilidades ---
bcrypt>=4.1,<6.0        # hash de contraseñas del panel (probado con 4.x y 5.x)
requests>=2.31,<3.0     # notificaciones (Telegram/ntfy) y test de velocidad

# ============================================================================
# Dependencias de fases siguientes (NO requeridas para la Fase 1).
# Se dejan documentadas; descomenta cuando se implemente cada fase.
# ============================================================================
# --- Fase 2: control activo (limite de ancho de banda por WinDivert) ---
# pydivert>=2.1,<3.0        # bindings de WinDivert (solo Windows)
#
# --- Fase 3: seguridad y notificaciones ---
# passlib[bcrypt]>=1.7      # hash de contraseñas para la autenticacion
# python-multipart>=0.0.9   # formularios de login
# requests>=2.31            # Telegram / ntfy / speedtest
# speedtest-cli>=2.1        # test de velocidad de internet
# wakeonlan>=3.1            # Wake-on-LAN
